apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: vigo-auth-organisation-selector-frontend
spec:
  entryPoints:
    - web
  routes:
#    - match: Host(`betaling-viken.felleskomponent.no`) && PathPrefix(`/api`)
#      kind: Rule
#      services:
#        - name: fint-betaling-frontend
#          port: 8080